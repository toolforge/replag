config_version: v1beta1
source_url: https://gitlab.wikimedia.org/toolforge-repos/replag/-/raw/master/toolforge.yaml
components:
  tool-replag:
    build:
      repository: https://gitlab.wikimedia.org/toolforge-repos/replag.git
      ref: HEAD
    run:
      command: web
      mount: none
      port: 8080
      publish: /
